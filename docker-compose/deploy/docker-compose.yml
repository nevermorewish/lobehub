name: lobehub
services:
  lobe:
    build:
      context: ../..
      dockerfile: Dockerfile
    image: crpi-dzjyl2rfnlfugj1m.cn-shanghai.personal.cr.aliyuncs.com/frogclaw/lobehub:latest
    container_name: lobehub
    ports:
      - '3210:3210'
    depends_on:
      postgresql:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      - 'KEY_VAULTS_SECRET=${KEY_VAULTS_SECRET}'
      - 'AUTH_SECRET=${AUTH_SECRET}'
      - 'JWKS_KEY=${JWKS_KEY}'
      - 'APP_URL=http://106.55.12.78:3210'
      - 'DATABASE_URL=${DATABASE_URL}'
      - 'S3_ENDPOINT=https://tos-s3-cn-beijing.volces.com'
      - 'S3_BUCKET=huanxing'
      - 'S3_ACCESS_KEY_ID=${RUSTFS_ACCESS_KEY}'
      - 'S3_SECRET_ACCESS_KEY=${RUSTFS_SECRET_KEY}'
      - 'S3_ENABLE_PATH_STYLE=1'
      - 'LLM_VISION_IMAGE_USE_BASE64=1'
      - 'S3_SET_ACL=0'
      - 'SEARXNG_URL=http://searxng:8080'
      - 'REDIS_URL=redis://redis:6210'
      - 'REDIS_PREFIX=lobechat'
      - 'REDIS_TLS=0'
      - 'ADMIN_SERVICE_URL=http://admin:3211'
      - 'ADMIN_INTEGRATION_TOKEN=${ADMIN_INTEGRATION_TOKEN}'
    restart: always
    networks:
      - lobe-network

  admin:
    build:
      context: ../../admin
      dockerfile: Dockerfile
    image: lobehub-go-admin:local
    ports:
      - '${ADMIN_BIND_ADDRESS:-127.0.0.1}:${ADMIN_PORT:-3211}:3211'
    depends_on:
      postgresql:
        condition: service_healthy
    environment:
      DATABASE_URL: ${ADMIN_DATABASE_URL:?Set ADMIN_DATABASE_URL to the LobeHub PostgreSQL database}
      ADMIN_USERNAME: ${ADMIN_USERNAME:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?Set ADMIN_PASSWORD}
      ADMIN_PUBLIC_URL: ${ADMIN_PUBLIC_URL:-http://localhost:3211}
      ADMIN_ENCRYPTION_KEY: ${ADMIN_ENCRYPTION_KEY:?Set ADMIN_ENCRYPTION_KEY}
      ADMIN_INTEGRATION_TOKEN: ${ADMIN_INTEGRATION_TOKEN:?Set ADMIN_INTEGRATION_TOKEN}
      PORT: '3211'
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - lobe-network

  postgresql:
    image: paradedb/paradedb:latest-pg17
    container_name: lobe-postgres
    # Optional: Remove or change these ports if:
    # - You don't need external database access
    # - The ports conflict with other services on your host
    ports:
      - '5200:5200'
    volumes:
      - './data:/var/lib/postgresql/data'
    command: ['postgres', '-p', '5200']
    environment:
      - 'POSTGRES_DB=lobechat'
      - 'POSTGRES_PASSWORD=${POSTGRES_PASSWORD}'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -p 5200']
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - lobe-network

  redis:
    image: redis:7-alpine
    container_name: lobe-redis
    # Optional: Remove or change these ports if:
    # - You don't need external access
    # - The ports conflict with other services on your host
    ports:
      - '6210:6210'
    command: redis-server --port 6210 --save 60 1000 --appendonly yes
    volumes:
      - 'redis_data:/data'
    healthcheck:
      test: ['CMD', 'redis-cli', '-p', '6210', 'ping']
      interval: 5s
      timeout: 3s
      retries: 5
    restart: always
    networks:
      - lobe-network

  searxng:
    image: searxng/searxng
    container_name: lobe-searxng
    volumes:
      - './searxng-settings.yml:/etc/searxng/settings.yml'
    environment:
      - 'SEARXNG_SETTINGS_FILE=/etc/searxng/settings.yml'
    restart: always
    networks:
      - lobe-network

networks:
  lobe-network:
    driver: bridge

volumes:
  redis_data:
    driver: local
